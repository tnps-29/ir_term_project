# เอกสารอธิบายรายละเอียดสถาปัตยกรรมและอัลกอริทึมของระบบ

เอกสารฉบับนี้จัดทำขึ้นเพื่ออธิบายรายละเอียดของเทคโนโลยีที่เลือกใช้ (Tech Stack) การทำงานของอัลกอริทึมต่างๆ ภายในระบบ "Automatic Software Defect Retrieval and Classification System" ตลอดจนอธิบายวิธีการวัดผลและตัวชี้วัดที่ใช้

---

## ขอบเขตของโครงการ (Project Scope)

**1. ขอบเขตด้านข้อมูล (Data Scope)**

- ใช้ชุดข้อมูลรายงานข้อผิดพลาดของซอฟต์แวร์ (Software Defect Reports) จากไฟล์ `defects_classification.csv`
- นำข้อความจากหลายส่วน (Title, Issue found, Step to reproduce, Actual result, Expected result) มารวมกันเพื่อใช้เป็น Feature ในการวิเคราะห์
- รองรับข้อมูลที่เป็นภาษาไทยและภาษาอังกฤษที่ปะปนกันในประโยค

**2. ขอบเขตด้านการจำแนกประเภท (Classification Scope)**

- อ้างอิงการจัดหมวดหมู่ตามมาตรฐานสากล **IEEE 1044-2009** โดยจำแนกออกเป็น 4 มิติ ได้แก่:
  1. **Defect Type** (ประเภทของข้อผิดพลาด เช่น Logic, Interface, Data)
  2. **Defect Effect** (ผลกระทบ เช่น Functionality, Security, Usability)
  3. **Severity** (ความรุนแรง เช่น Critical, Major, Minor)
  4. **Defect Mode** (ลักษณะที่ผิดพลาด เช่น Missing, Wrong, Extra)

**3. ขอบเขตด้านเทคนิคและอัลกอริทึม (Technical Scope)**

- พัฒนาส่วนแสดงผลเป็น Web Application ด้วย **Streamlit** เพื่อให้ใช้งานและทดสอบได้ง่าย
- มีระบบกรองข้อมูล Rule-based (Blacklist) เพื่อปัดตกรุ่นรายงานที่ไม่ใช่บั๊ก (เช่น Tasks/Enhancements)
- ใช้ **PyThaiNLP** และ **NLTK** สำหรับกระบวนการ Text Preprocessing (Tokenization & Stopwords removal)
- ใช้เทคนิค **TF-IDF** ร่วมกับ **Rocchio Algorithm (Nearest Centroid)** และคำนวณระยะห่างด้วย **Cosine Similarity** ในการสร้างแบบจำลอง (Machine Learning Model)

**4. ขอบเขตด้านการวัดผล (Evaluation Scope)**

- ประเมินประสิทธิภาพของโมเดลด้วยการทดสอบแบบ **80/20 Train-Test Split**
- ชี้วัดผลลัพธ์การทำนายด้วยค่า Accuracy, Precision, Recall, F1-Score และแสดงผลการกระจายตัวผ่าน Confusion Matrix

**5. ข้อจำกัดของโครงการ (Out of Scope)**

- ไม่ครอบคลุมการเชื่อมต่อ API เพื่อดึงข้อมูลแบบ Real-time จากระบบ Issue Tracker ภายนอก (เช่น Jira, GitHub Issues)
- แบบจำลองมุ่งเน้นที่ Information Retrieval แบบคลาสสิก (Rocchio) ยังไม่รองรับเทคนิค Deep Learning หรือ Large Language Models (LLMs)

---

## 1. Tech Stack ที่ใช้และเหตุผลที่เลือกใช้

ระบบนี้ถูกพัฒนาขึ้นมาในรูปแบบ Web Application โดยเน้นเทคโนโลยีทางด้าน Data Science และ Natural Language Processing (NLP) ของ Python

1. **Python 3**: เป็นภาษาโปรแกรมมิ่งหลัก เนื่องจากเป็นภาษามาตรฐานสำหรับงานด้าน Machine Learning และมี Community ที่สนับสนุน Library ด้านการประมวลผลข้อความที่ดีที่สุด
2. **Streamlit**: Web Framework สำหรับสร้าง Interactive Web App ในภาษา Python
   - _เหตุผล_: ช่วยให้สามารถสร้าง UI ได้อย่างรวดเร็วโดยไม่ต้องเขียน HTML/CSS/JS หรือแยก Backend/Frontend อย่างซับซ้อน นอกจากนี้ยังมีระบบ Caching (`@st.cache_data`, `@st.cache_resource`) ซึ่งสำคัญมากสำหรับ ML Model เพราะช่วยให้ไม่ต้องเทรนโมเดลใหม่หรือโหลดข้อมูลใหม่ทุกครั้งที่ผู้ใช้กดปุ่ม
3. **Pandas & NumPy**: Library สำหรับจัดการข้อมูล
   - _เหตุผล_: ใช้ในการโหลดไฟล์ CSV, การรวบรวมคอลัมน์ข้อความหลายๆ คอลัมน์ (Title, Issue found, Expected result) ให้กลายเป็นข้อความยาวๆ ข้อความเดียว (Feature Aggregation) และจัดการกับข้อมูลที่ว่างเปล่า (Missing values) ได้อย่างมีประสิทธิภาพ
4. **PyThaiNLP & NLTK**: Library สำหรับ NLP
   - _เหตุผล (PyThaiNLP)_: Library ของฝรั่ง (เช่น NLTK, spaCy) ไม่สามารถตัดคำภาษาไทยได้เนื่องจากภาษาไทยไม่ใช้ Spacebar เป็นตัวแบ่งคำ เราจึงใช้ engine `newmm` (Maximum Matching) ของ PyThaiNLP ที่มีความแม่นยำสูงในการตัดคำ และใช้ฐานข้อมูล Thai Stopwords ของมัน
   - _เหตุผล (NLTK)_: ใน Defect Report มักมีภาษาอังกฤษปนอยู่ (เช่น error, bug, button) จึงใช้ NLTK เข้ามาช่วยกรอง Stopwords ภาษาอังกฤษทิ้งไป
5. **Scikit-learn**: Library หลักสำหรับ Machine Learning
   - _เหตุผล_: มีการติดตั้ง `TfidfVectorizer` สำหรับดึง Feature ข้อความ และมีระบบวัดผล (Metrics) ที่พร้อมใช้งาน

---

## 2. ขั้นตอนการทำงานของอัลกอริทึม (Algorithm Workflow)

ระบบมีการประมวลผลข้อความดิบให้กลายเป็นผลการทำนาย โดยผ่านขั้นตอนและอัลกอริทึมหลักๆ ดังนี้:

### 2.1 Rule-Based Filtering (การกรองข้อมูลด้วยกฎ)

- **การทำงาน**: ก่อนจะส่งข้อความเข้าโมเดล ML เราใช้ Blacklist Dictionary เพื่อตรวจสอบหาคีย์เวิร์ดที่เป็น "Task" หรือ "Enhancement" (เช่น "เพิ่ม field", "add", "ปรับปรุง")
- **เหตุผล**: เพื่อกรองสิ่งที่ไม่ใช่ Defect (Non-defects) ออกไปตั้งแต่เนิ่นๆ ลดภาระและเพิ่มความแม่นยำให้โมเดล

### 2.2 Text Preprocessing (NLP)

- **การทำงาน**: ลบอักขระพิเศษ (Special Characters), ตัดคำ (Tokenization) ด้วยเครื่องมือ `newmm`, และลบคำหยุด (Stopword Removal) ที่ไม่มีความหมายในการแบ่งแยกประโยค (เช่น "ที่", "ซึ่ง", "อัน", "และ", "a", "the")
- **เหตุผล**: ลด "Noise" ในฐานข้อมูล เพื่อให้โมเดลโฟกัสเฉพาะคำที่มีนัยสำคัญต่อชนิดของบั๊ก

### 2.3 TF-IDF Vectorizer (Term Frequency - Inverse Document Frequency)

- **ที่มา**: เป็นเทคนิคดั้งเดิมในวงการ Information Retrieval (IR)
- **การทำงาน**: แปลงข้อความ (Tokens) ให้กลายเป็นตัวเลข (Vector) โดยให้ความสำคัญตามสมการทางคณิตศาสตร์:
  - **TF (Term Frequency)**: คำนี้ปรากฏในรายงานฉบับนี้บ่อยแค่ไหน (ยิ่งบ่อย ยิ่งสำคัญ)
  - **IDF (Inverse Document Frequency)**: คำนี้ปรากฏในเอกสารฉบับอื่นทั่วๆ ไปบ่อยแค่ไหน (ถ้าเจอบ่อยในทุกรายงาน เช่นคำว่า "ระบบ", "โปรแกรม" น้ำหนักความสำคัญของคำนี้จะถูกดรอปลง)
- **เหตุผลที่ใช้**: การใช้ TF-IDF ดีกว่าการนับคำปกติ (Bag of Words) เพราะมันช่วยดึงคำที่เป็น "คีย์เวิร์ดเฉพาะ" ของบั๊กประเภทนั้นๆ ออกมาได้ชัดเจนกว่า

### 2.4 Rocchio Algorithm (Nearest Centroid)

- **ที่มา**: ถูกคิดค้นโดย Joseph Rocchio สำหรับประยุกต์ใช้ในระบบ Information Retrieval แบบดั้งเดิม (Vector Space Model)
- **การทำงาน**:
  1. **Phase การเรียนรู้ (Train)**: นำ Vector (TF-IDF) ของข้อมูลทั้งหมดมารวมกันตามแต่ละคลาส แล้วหาจุดกึ่งกลาง (Mean Vector / Centroid) เพื่อเป็นตัวแทนของคลาสนั้น เช่น จุดศูนย์กลางของคลาส "Logic"
  2. **Phase การทำนาย (Predict)**: เมื่อมีข้อความบั๊กใหม่เข้ามา ระบบจะแปลงเป็น Vector จากนั้นจะคำนวณระยะห่างแบบ **Cosine Similarity** ระหว่างเวกเตอร์ใหม่นี้กับจุดศูนย์กลางของทุกคลาส คลาสใดที่มีค่าความคล้ายคลึง (Similarity) เข้าใกล้ 1 มากที่สุด ข้อความนั้นจะถูกจัดให้อยู่ในคลาสนั้น
- **เหตุผลที่ใช้**: เป็น Algorithm ที่เรียบง่าย ทำงานได้รวดเร็ว และมีประสิทธิภาพดีมากเมื่อจัดการกับข้อมูลข้อความ (Text) ที่มีมิติของข้อมูล (Dimensionality) สูงมากๆ

---

## 3. วิธีการวัดผลและตัวชี้วัด (Evaluation Metrics)

ระบบของเราประเมินผลผ่าน **"Model Evaluation Dashboard"** โดยใช้การแบ่งชุดข้อมูลแบบ **80/20 Train-Test Split** (นำข้อมูล 80% ไปหาจุด Centroid และใช้ข้อมูลอีก 20% ที่โมเดลไม่เคยเห็นมาเป็นข้อสอบ)

ตัวชี้วัดทางคณิตศาสตร์ที่ระบบใช้แสดงผล ได้แก่:

### 3.1 Accuracy (ความแม่นยำรวม)

- **คืออะไร**: สัดส่วนของรายงานทั้งหมดที่โมเดลทายถูก (ทายถูก / ทั้งหมด)
- **ผลลัพธ์ที่ควรจะเป็น**: ยิ่งเปอร์เซ็นต์สูงยิ่งดี แต่ข้อควรระวังคือ Accuracy ไม่ใช่ตัวชี้วัดที่ดีที่สุดหากคลาสในข้อมูลมีจำนวนไม่เท่ากัน (Imbalanced Data)

### 3.2 Precision, Recall, และ F1-Score

- **Precision**: ในบรรดาสิ่งที่โมเดลทายว่าเป็นคลาส A, มันเป็นคลาส A จริงๆ เท่าไหร่? (สะท้อนว่าโมเดลมั่วหรือไม่)
- **Recall**: จากคลาส A ที่มีอยู่จริงทั้งหมด, โมเดลสามารถหาตัวมันเจอและทายถูกเท่าไหร่? (สะท้อนว่าโมเดลทำหลุดรอดไปเยอะไหม)
- **F1-Score**: ค่าเฉลี่ยฮาร์มอนิกของ Precision และ Recall
- **ผลลัพธ์ที่ควรจะเป็น**: ค่าจะอยู่ระหว่าง 0 ถึง 1 (หรือ 0% - 100%) สำหรับระบบ Classification ของซอฟต์แวร์ ควรโฟกัสที่ค่า **F1-Score** เป็นหลัก เพราะจะสะท้อนความสมดุลและความเก่งของโมเดลได้ดีที่สุดแม้ว่าข้อมูลแต่ละประเภทบั๊กจะมีจำนวนรายงานไม่เท่ากัน

### 3.3 Confusion Matrix

- **คืออะไร**: ตารางเมทริกซ์ 2 มิติที่เปรียบเทียบระหว่าง "ความเป็นจริง (True Label)" (แกน Y) กับ "สิ่งที่โมเดลทำนาย (Predicted Label)" (แกน X)
- **ผลลัพธ์ที่ควรจะเป็น (แบบคร่าวๆ)**:
  - ตัวเลข (หรือสีที่เข้มที่สุด) ควรจะไปกระจุกรวมกันอยู่ที่ **"แนวทแยงมุม (Diagonal)"** จากซ้ายบนลงขวาล่าง เพราะแนวทแยงมุมหมายถึง ทายตรงกับความจริง
  - ตัวเลขที่หลุดออกไปนอกแนวทแยงมุม คือ "ความสับสน" ของโมเดล ตัวอย่างเช่น หากมีตัวเลขอยู่นอกแนวทแยงมุมตรงจุดตัดระหว่างคลาส Logic และ Interface แสดงว่าโมเดลและอัลกอริทึมยังแยกคำศัพท์ระหว่างสองคลาสนี้ไม่ออก ซึ่งมีประโยชน์มากในการนำไปใช้วิเคราะห์เพื่อเพิ่ม Stopwords หรือปรับปรุงข้อมูลดิบในอนาคต

---

## 4. การวิเคราะห์ผลลัพธ์จาก Model Evaluation (อ้างอิงจากชุดทดสอบ 30 รายการ)

จากการรันประเมินผลผ่าน Dashboard แบบ 80/20 Train-Test Split สามารถวิเคราะห์ที่มาและสาเหตุของผลลัพธ์ในแต่ละ Dimension จากภาพที่แนบมาได้ดังนี้:

### 4.1 Dimension: MODE (Accuracy 73.33%) - ทำผลงานได้ดีที่สุด

- **ผลลัพธ์**: โมเดลสามารถแยกคลาส "Missing" และ "Wrong" ได้ค่อนข้างแม่นยำ (F1-score 0.80 และ 0.67 ตามลำดับ)
- **ที่มาและสาเหตุ**:
  - **ความต่างของชุดคำ (Linguistic Distinctiveness)**: ภาษาที่ผู้ใช้ใช้เขียนรายงานสำหรับ 2 คลาสนี้มีความแตกต่างกันอย่างชัดเจน เช่น อาการ Missing มักใช้คำว่า "ไม่พบ", "หายไป", "ไม่แสดง" ในขณะที่อาการ Wrong มักใช้คำว่า "ผิด", "เพี้ยน", "ทับซ้อน"
  - อัลกอริทึม TF-IDF สามารถจับน้ำหนักของคำคีย์เวิร์ดเหล่านี้ได้อย่างชัดเจน ทำให้จุด Centroid ของทั้งสองคลาสอยู่ห่างกันใน Vector Space

### 4.2 Dimension: EFFECT (Accuracy 53.33%)

- **ผลลัพธ์**: โมเดลทายคลาส Security ได้แม่นยำ (Recall 1.00) และ Functionality ได้ปานกลาง แต่ไม่สามารถทายคลาส Performance ได้เลย (F1-score 0.00)
- **ที่มาและสาเหตุ**:
  - **ปัญหาข้อมูลไม่สมดุล (Class Imbalance)**: คลาส Performance มีข้อมูลในชุดทดสอบเพียง 2 ตัว (Support=2) และคาดว่าในชุดฝึกสอน (Train set) ก็มีน้อยมาก ทำให้โมเดลเรียนรู้คำศัพท์ของกลุ่ม Performance ได้ไม่เพียงพอ จุด Centroid จึงไม่แข็งแรง
  - คำศัพท์ของคลาส Security มักจะเป็นคำเฉพาะเจาะจงที่โดดเด่น เช่น "รหัสผ่าน", "เข้ารหัส", "สิทธิ์" (Unique Vocabulary) ทำให้แยกออกง่ายแม้ข้อมูลจะน้อย

### 4.3 Dimension: TYPE (Accuracy 50.00%)

- **ผลลัพธ์**: คลาส Interface ทำผลงานได้ดีที่สุด (F1-score 0.60) แต่โมเดลมักจะสับสนและทายคลาส Data และ Logic ผิดเป็น Interface (ดูจาก Confusion Matrix ที่ Data 4 ตัวและ Logic 5 ตัวถูกทายผิดเป็น Interface)
- **ที่มาและสาเหตุ**:
  - **ความกำกวมของเนื้อหา (Linguistic Ambiguity)**: ผู้ใช้งานมักจะรายงานความผิดปกติของ Data หรือ Logic จาก **"สิ่งที่เห็นบนหน้าจอ"** เช่น "ข้อมูล(Data)สรุปยอด แสดงผลบนหน้าเว็บ(Interface)ผิดพลาด"
  - ทำให้มีคำศัพท์หมวด UI ปะปนอยู่ในบั๊กประเภท Data/Logic สูงมาก โมเดลที่อาศัยเพียงการนับคำ (TF-IDF) โดยไม่เข้าใจบริบทเชิงลึก (Context) จึงถูกหลอกให้ทายเป็น Interface ได้ง่าย

### 4.4 Dimension: SEVERITY (Accuracy 46.67%) - อ่อนแอที่สุด

- **ผลลัพธ์**: โมเดลมีความสับสนอย่างหนักระหว่าง "Major" และ "Minor" (ทาย Minor เป็น Major 4 ครั้ง และทาย Major เป็น Minor ถึง 10 ครั้ง)
- **ที่มาและสาเหตุ**:
  - ความรู้สึกส่วนตัว (Subjectivity): ระดับความรุนแรง (Severity) มักขึ้นอยู่กับความรู้สึกของผู้แจ้งบั๊ก คำศัพท์ที่ใช้บรรยายบั๊ก Major กับ Minor อาจจะใช้ชุดคำเดียวกันเป๊ะ (เช่น "ปุ่มกดไม่ได้") แต่อันนึงเกิดที่หน้าชำระเงิน (Major) อีกอันเกิดที่หน้านโยบาย (Minor)
  - เนื่องจากคำศัพท์ (Tokens) ของทั้งสองคลาสมีความคล้ายคลึงกันมาก จุด Centroid ของ Rocchio Algorithm สำหรับสองคลาสนี้จึงแทบจะทับซ้อนกันใน Vector Space ทำให้โมเดลไม่สามารถหาเส้นแบ่งที่ชัดเจนได้

---

## 5. ข้อจำกัดของโครงการ (Limitations)

1. **ปัญหาคำศัพท์ใหม่ (Out-of-Vocabulary - OOV):** อัลกอริทึม TF-IDF อาศัยการนับความถี่คำศัพท์ที่มีอยู่ในฐานข้อมูล (Training Set) เท่านั้น หากผู้ใช้ป้อนรายงานที่ใช้คำศัพท์ใหม่ๆ (เช่น ฟีเจอร์ใหม่, รหัส Error โค้ดแปลกๆ) โมเดลจะไม่สามารถดึง Feature ได้ ทำให้ค่า Similarity ต่ำ และต้องใช้วิธีเดาสุ่ม
2. **ไม่เข้าใจบริบทของประโยค (Lack of Contextual Understanding):** โมเดลทำงานแบบ "Bag of Words" คือมองเห็นแค่คำโดดๆ เช่น "ปุ่ม", "ไม่", "ทำงาน" แต่มันไม่เข้าใจโครงสร้างประโยคหรือการปฏิเสธ (Negation) ทำให้ประโยค "ทำงานปกติ" กับ "ทำงานไม่ปกติ" อาจถูกมองว่าคล้ายกันเพราะมีคำว่า "ทำงาน" เหมือนกัน
3. **ปัญหาข้อมูลไม่สมดุล (Class Imbalance):** ในโลกความเป็นจริง บั๊กบางประเภทเกิดขึ้นน้อยมาก (เช่น Performance, Security) ส่งผลให้จุด Centroid ของกลุ่มเหล่านี้มีข้อมูลมาซัพพอร์ตน้อย ทำให้ความแม่นยำต่ำ
4. **ความเอนเอียงจากคน (Human Bias & Subjective Labels):** ป้ายกำกับ (Labels) ในไฟล์ CSV อ้างอิงจากวิจารณญาณของมนุษย์ หากในอดีตทีมงานแยกความแตกต่างระหว่าง Major กับ Minor ได้ไม่คงเส้นคงวา โมเดลก็จะเรียนรู้และทำซ้ำความสับสนนั้นต่อไป

## 6. แนวทางพัฒนาในอนาคต (Future Work)

1. **ใช้ Word Embeddings และ Contextual Models:** อัปเกรดจาก TF-IDF ไปใช้เทคนิค NLP ขั้นสูงที่เข้าใจความหมายและบริบทของคำศัพท์ (Semantic Context) เช่น Word2Vec, FastText หรือเปลี่ยนไปใช้โมเดลระดับ Transformer อย่าง BERT (เช่น WangchanBERTa สำหรับภาษาไทย)
2. **การจัดการข้อมูลที่ไม่สมดุล (Data Augmentation):** นำเทคนิคอย่าง SMOTE (Synthetic Minority Over-sampling Technique) มาใช้สร้างข้อมูลจำลองสำหรับคลาสที่มีน้อย (เช่น บั๊กหมวด Performance) หรือการสุ่มข้อมูลคลาสที่เยอะให้ลดลง (Under-sampling)
3. **ระบบเรียนรู้อย่างต่อเนื่อง (Active Learning / Feedback Loop):** เพิ่มปุ่มบนหน้า Web UI ให้ผู้ใช้งานกดรับรองว่า "โมเดลทำนายถูก" หรือ "โมเดลทำนายผิด" นำข้อมูล (Feedback) เหล่านี้บันทึกกลับเข้าไปในฐานข้อมูล เพื่อให้โมเดลสามารถ Retrain และเก่งขึ้นได้เองเมื่อเวลาผ่านไป
4. **เชื่อมต่อกับระบบจริง (API Integration):** พัฒนาระบบให้สามารถดึงข้อมูลตั๋วแจ้งซ่อม (Tickets) จากระบบ Issue Tracker ระดับโลก เช่น Jira, GitLab, หรือ GitHub Issues แบบเรียลไทม์ และทำเครื่องหมาย (Auto-label) ประเภทบั๊กให้โดยอัตโนมัติ

---

## 7. บทสรุป (Conclusion)

โครงการพัฒนา "ระบบจำแนกประเภทข้อผิดพลาดของซอฟต์แวร์อัตโนมัติ (Automatic Software Defect Classification)" นำไปสู่ข้อสรุปที่สำคัญดังต่อไปนี้:

1. **ความเป็นไปได้ของเทคนิคดั้งเดิม (Feasibility of Traditional IR):** 
   โครงการนี้พิสูจน์ให้เห็นว่าเทคนิคดั้งเดิมในวงการ Information Retrieval อย่าง **TF-IDF และ Rocchio Algorithm** ยังคงมีประสิทธิภาพ สามารถใช้จัดหมวดหมู่รายงานบั๊กภาษาไทยตามมาตรฐาน IEEE 1044-2009 ได้จริง โดยมีข้อดีคือโมเดลมีขนาดเล็ก ทำงานได้รวดเร็วแบบ Real-time และสามารถอธิบายที่มาที่ไปของการตัดสินใจผ่านค่า Cosine Similarity ได้อย่างเป็นรูปธรรม
2. **ความสำเร็จจากชุดคำศัพท์ที่ชัดเจน (Success in Linguistic Distinctiveness):** 
   ระบบทำผลงานได้ดีเยี่ยมในมิติที่มีการใช้ชุดคำศัพท์เฉพาะกลุ่มที่แยกขาดจากกันอย่างชัดเจน เช่น มิติ Defect Mode (การแยก Missing ออกจาก Wrong) หรือมิติ Effect ด้าน Security สิ่งนี้ชี้ให้เห็นว่าหัวใจสำคัญของโมเดลนี้คือกระบวนการ Text Preprocessing (การตัดคำและลบ Stopwords) ที่มีคุณภาพ
3. **การค้นพบปัญหาความกำกวมของมนุษย์ (Uncovering Human Subjectivity):** 
   ผลการประเมินช่วยสะท้อนให้เห็นข้อจำกัดของการแจ้งบั๊กโดยมนุษย์ เช่น ในมิติความรุนแรง (Severity) โมเดลแยก Major และ Minor ไม่ออก เนื่องจากผู้ใช้งานใช้คำอธิบายบั๊กเหมือนกันทุกประการแต่ให้ระดับความรุนแรงต่างกันตามความรู้สึกส่วนตัว รวมถึงปัญหาข้อมูลไม่สมดุล (Class Imbalance) ที่ทำให้โมเดลไม่รู้จักบั๊กหายาก
4. **ก้าวต่อไปสู่ระบบองค์กร (Final Takeaway):** 
   แม้ระบบปัจจุบันจะทำหน้าที่เป็นโมเดลต้นแบบ (Prototype) ที่สมบูรณ์แบบ แต่หากต้องการนำไปใช้งานจริงในระดับองค์กร (Enterprise Production) จะต้องนำแนวทางพัฒนาในอนาคต (ข้อ 6) มาปรับใช้ โดยเฉพาะการผสานรวมโมเดลที่เข้าใจบริบท (Contextual Models) เข้ากับการสร้างระบบรับ Feedback จากวิศวกรผู้ใช้งานจริง เพื่อให้เกิดความแม่นยำสูงสุดและลดภาระในการคัดแยกบั๊กได้อย่างแท้จริง
